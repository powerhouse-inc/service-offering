--- a/document-models/subscription-instance/schema.graphql
+++ b/document-models/subscription-instance/schema.graphql
@@ -43,6 +43,7 @@ type SubscriptionInstanceState {
     autoRenew: Boolean!
     operatorNotes: String
     budget: BudgetCategory
+    selectedBillingCycle: BillingCycle
     nextBillingDate: DateTime
     projectedBillAmount: Amount_Money
     projectedBillCurrency: Currency
@@ -58,6 +59,7 @@ type ServiceGroup {
     id: OID!
     optional: Boolean!
     name: String!
+    setupCost: SetupCost
     recurringCost: RecurringCost
     services: [Service!]!
 }
@@ -180,6 +182,9 @@ input InitializeServiceGroupInput {
     id: OID!
     name: String!
     optional: Boolean!
+    setupAmount: Amount_Money
+    setupCurrency: Currency
+    setupBillingDate: DateTime
     recurringAmount: Amount_Money
     recurringCurrency: Currency
     recurringBillingCycle: BillingCycle
@@ -196,6 +201,7 @@ input InitializeSubscriptionInput {
     tierPricingOptionId: OID
     tierPrice: Amount_Money
     tierCurrency: Currency
+    selectedBillingCycle: BillingCycle
     resourceId: PHID
     resourceLabel: String
     resourceThumbnailUrl: URL
@@ -357,6 +363,9 @@ input AddServiceGroupInput {
     groupId: OID!
     name: String!
     optional: Boolean!
+    setupAmount: Amount_Money
+    setupCurrency: Currency
+    setupBillingDate: DateTime
     recurringAmount: Amount_Money
     recurringCurrency: Currency
     recurringBillingCycle: BillingCycle
@@ -392,6 +401,9 @@ input UpdateServiceGroupCostInput {
     groupId: OID!
+    setupAmount: Amount_Money
+    setupCurrency: Currency
+    setupBillingDate: DateTime
     recurringAmount: Amount_Money
     recurringCurrency: Currency
     recurringBillingCycle: BillingCycle
